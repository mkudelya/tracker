{% extends "OroTrackerBundle::layout.html.twig" %}

{% block description %}{{ 'user.users_list'|trans({}, 'OroTrackerBundle') }}{% endblock %}

{% block page_actions %}
[ <a href="{{ path('fos_user_registration_register') }}">{{ 'user.add'|trans({}, 'OroTrackerBundle') }}</a> ]
{% endblock %}

{% block content %}
    {% if users|length > 0 %}
        <table width="850">
            <thead>
                <tr>
                    <th width="5%">{{ 'grid_title.id'|trans({}, 'OroTrackerBundle') }}</th>
                    <th width="20%">{{ 'grid_title.email'|trans({}, 'OroTrackerBundle') }}</th>
                    <th width="20%">{{ 'grid_title.username'|trans({}, 'OroTrackerBundle') }}</th>
                    <th width="10%">{{ 'grid_title.full_name'|trans({}, 'OroTrackerBundle') }}</th>
                    <th width="10%">{{ 'grid_title.actions'|trans({}, 'OroTrackerBundle') }}</th>
                </tr>
            <thead>
            <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.getId() }}</td>
                    <td>{{ user.getEmail() }}</td>
                    <td>{{ user.getUsername() }}</td>
                    <td>{{ user.getFullname() }}</td>
                    <td><a href="{{ path('_oro_user_edit', {'id': user.getId()}) }}">{{ 'grid.edit'|trans({}, 'OroTrackerBundle') }}</a>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% else %}
        {{ 'user.users_not_found'|trans({}, 'OroTrackerBundle') }}
    {% endif %}
{% endblock %}

